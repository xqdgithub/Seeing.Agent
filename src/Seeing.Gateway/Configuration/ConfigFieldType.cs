namespace Seeing.Gateway.Configuration;

/// <summary>
/// Gateway Channel 配置字段 UI 类型
/// </summary>
public enum ConfigFieldType
{
    String,
    Secret,
    Url,
    Number,
    Boolean,
    Enum,
    StringList,
    TextArea
}
